# penguinOS, 16 MB, two app slots. The roomy version of partitions.csv.
#
# Same shape and none of the tension: a 16 MB part can carry the app twice at
# 4 MB a slot and still leave 8 MB of filesystem, so nothing here is a
# compromise and the numbers are round on purpose.
#
#   nvs        24 KB
#   phy_init    4 KB
#   otadata     8 KB       two sectors; the bootloader alternates them
#   ota_0    4,096 KB      app slot A
#   ota_1    4,096 KB      app slot B
#   int      8,128 KB      LittleFS
#
# 4 MB a slot against a ~1.5 MB image is 2.7x headroom. The 4 MB boards run
# 8%, and this file exists so the difference is visible rather than inferred.
# Name,     Type, SubType,  Offset,    Size,
nvs,        data, nvs,      0x9000,    0x6000,
phy_init,   data, phy,      0xF000,    0x1000,
otadata,    data, ota,      0x10000,   0x2000,
ota_0,      app,  ota_0,    0x20000,   0x400000,
ota_1,      app,  ota_1,    0x420000,  0x400000,
int,        data, littlefs, 0x820000,  0x7E0000,
